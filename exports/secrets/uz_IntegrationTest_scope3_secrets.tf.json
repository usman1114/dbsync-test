{
    "locals": {
        "databricks_secret_uz-IntegrationTest-scope3_for_each_var": {
            "db_password": {
                "key": "db_password",
                "scope": "${databricks_secret_scope.databricks_secret_scope_uz_IntegrationTest_scope3.id}",
                "string_value": "${var.uz_IntegrationTest_scope3_db_password_var}"
            }
        }
    },
    "resource": {
        "databricks_secret": {
            "uz_IntegrationTest_scope3_secrets": {
                "for_each": "${local.databricks_secret_uz-IntegrationTest-scope3_for_each_var}",
                "key": "${each.value.key}",
                "scope": "${each.value.scope}",
                "string_value": "${each.value.string_value}"
            }
        }
    },
    "variable": {
        "uz_IntegrationTest_scope3_db_password_var": {}
    }
}